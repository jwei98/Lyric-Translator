<%= form_for(@translation) do |f| %>
  <% if @translation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@translation.errors.count, "error") %> prohibited this translation from being saved:</h2>

      <ul>
      <% @translation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, class: "control-label" %><br>
    <%= f.text_area :name, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :lyric, class: "control-label" %><br>
    <%= f.text_area :lyric, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :language, class: "control-label" %><br>
    <%= f.text_area :language, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :translation, class: "control-label" %><br>
    <%= f.text_area :translation, class: "form-control" %>
  </div>
  
  <%= f.submit "Save Translation", class: "btn btn-default" %>
<% end %>
